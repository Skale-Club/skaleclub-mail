# Plano: Sistema de Notificações Inbox (Webmail)

## Escopo

- **Usuários:** Usuários do Webmail (end-user)
- **Eventos Monitorados:** Eventos do servidor de email (bounces, held, spam alerts) e alertas administrativos
- **Entrega:** UI existente no MailLayout (ícone Bell com badge)

---

## Análise do Estado Atual

### UI Existente
- **Localização:** `src/components/mail/MailLayout.tsx:351-354`
- **Estado:** Botão Bell com badge vermelho fixo (hardcoded, sempre presente)
- **Problema:** Badge não reflete notificações reais; não há popup/dropdown para visualizar notificações

### Sistema de Eventos Existente
- Backend já dispara eventos: `message_bounced`, `message_held`, `spam_alert`
- Tabela `webhooks` registra webhooks para esses eventos
- Não existe tabela de "notificações de usuário" nem API para gerenciá-las

### Schema Atual
- `webhookEventEnum` define os tipos de evento
- `webhooks` table armazena webhooks (não notificações de usuário)
- Não há `notifications` table para o usuário do webmail

---

## Arquitetura do Sistema de Notificações

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  Evento SMTP   │───▶│  Background   │───▶│  Tabela        │
│ (bounce/held)  │    │  Worker       │    │  user_notifs   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                    │
┌─────────────────┐    ┌─────────────────┐          ▼
│  Alertas       │───▶│  Worker        │    ┌─────────────────┐
│  Admin         │    │  (quota/limite)│    │  API REST      │
└─────────────────┘    └─────────────────┘    │  /api/notif   │
                                             └─────────────────┘
                                                    │
                                             ┌─────┴─────┐
                                             ▼           ▼
                                      ┌──────────┐ ┌──────────┐
                                      │ Frontend │ │  Badge  │
                                      │  Bell    │ │  Count  │
                                      └──────────┘ └──────────┘
```

---

## Fase 1: Backend - Schema e API

### 1.1 Nova Tabela: `user_notifications`

```typescript
// src/db/schema.ts (adicionar)
export const userNotifications = pgTable('user_notifications', {
    id: uuid('id').primaryKey().defaultRandom(),
    userId: uuid('user_id').references(() => users.id).notNull(),
    type: text('type').notNull(), // 'bounce', 'held', 'spam_alert', 'quota_exceeded', 'limit_reached'
    title: text('title').notNull(),
    message: text('message').notNull(),
    metadata: jsonb('metadata').default({}), // { messageId, serverId, etc }
    read: boolean('read').default(false).notNull(),
    createdAt: timestamp('created_at').defaultNow().notNull(),
})

// Relations
export const userNotificationsRelations = relations(userNotifications, ({ one }) => ({
    user: one(users, {
        fields: [userNotifications.userId],
        references: [users.id],
    }),
}))
```

### 1.2 Nova Rota: `/api/notifications`

| Método | Endpoint | Descrição |
|--------|----------|-----------|
| GET | `/api/notifications` | Listar notificações do usuário (paginado) |
| GET | `/api/notifications/unread-count` | Retornar contagem de não lidas |
| PUT | `/api/notifications/:id/read` | Marcar como lida |
| PUT | `/api/notifications/read-all` | Marcar todas como lidas |
| DELETE | `/api/notifications/:id` | Deletar notificação |

### 1.3 Webhook Worker Atualizado

Em `src/server/lib/tracking.ts`, modificar `dispatchWebhook` para também criar registro na `user_notifications`:

```typescript
// Pseudocódigo - adaptar ao código existente
async function handleServerEvent(event: string, data: any) {
    const org = await getOrganization(data.organizationId)
    const orgUsers = await getOrgUsers(org.id)
    
    for (const user of orgUsers) {
        await db.insert(userNotifications).values({
            userId: user.id,
            type: eventToNotificationType(event),
            title: getNotificationTitle(event),
            message: getNotificationMessage(event, data),
            metadata: data,
        })
    }
}
```

---

## Fase 2: Frontend - Integração

### 2.1 hook: `useNotifications`

Criar `src/hooks/useNotifications.ts`:

```typescript
export function useNotifications() {
    // GET /api/notifications com React Query
}

export function useUnreadCount() {
    // GET /api/notifications/unread-count
}

export function useMarkAsRead() {
    // Mutation PUT /api/notifications/:id/read
}
```

### 2.2 Componente: NotificationBell

Modificar `MailLayout.tsx`:

1. Substituir badge hardcoded por dados reais
2. Adicionar dropdown/popover com lista de notificações

```tsx
// Estrutura do dropdown
<Popover>
    <PopoverTrigger asChild>
        <button>
            <Bell />
            {unreadCount > 0 && <Badge>{unreadCount}</Badge>}
        </button>
    </PopoverTrigger>
    <PopoverContent>
        <NotificationList />
    </PopoverContent>
</Popover>
```

### 2.3 Componente: NotificationList

- Listar últimas 10 notificações
- Cada item com: ícone do tipo, título, tempo relativo
- Ação de click → marca como lida
- Botão "Marcar todas como lidas"

---

## Fase 3: Tipos de Notificação

### 3.1 Eventos do Servidor de Email

| Evento | Tipo na Table | Título | Mensagem |
|--------|---------------|--------|----------|
| `message_bounced` | `bounce` | "Email bounced" | "Message to {recipient} bounced: {reason}" |
| `message_held` | `held` | "Message held" | "Message from {sender} is held: {reason}" |
| `spam_alert` | `spam_alert` | "Spam detected" | "Spam detected in message from {sender}" |

### 3.2 Alertas Administrativos

| Tipo | Título | Mensagem |
|------|--------|-----------|
| `quota_exceeded` | "Quota exceeded" | "Organization {name} exceeded storage quota" |
| `limit_reached` | "Daily limit reached" | "Daily send limit of {limit} reached" |
| `domain_verification_failed` | "Domain verification failed" | "Domain {domain} failed verification" |

---

## Fase 4: Melhorias Opcionais (V2)

1. **Notificações em tempo real:** WebSocket/SSE para updates instantâneos
2. **Configurações por usuário:** Notificar apenas alguns tipos
3. **Limpeza automática:** Auto-delete de notificações após 30 dias
4. **Email de resumo:** Notificações acumuladas/enviadas por email diário

---

## Ordem de Implementação Sugerida

1. Adicionar tabela `user_notifications` no schema
2. Criar rotas API `/api/notifications/*`
3. Integrar criação de notificações nos workers existentes
4. Criar hooks React Query
5. Atualizar MailLayout com dados reais
6. Adicionar popover/dropdown de notificações